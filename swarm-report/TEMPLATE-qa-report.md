# QA-отчёт — <branch> — <дата>

> Шаблон для `/post-pr-check`. Копируй в `swarm-report/<branch>-qa-report.md`, заполняй, не редактируй сам шаблон.
> Два уровня тестирования: **Level 1 — код** (pytest+vitest), **Level 2 — UI-smoke** (Playwright со скриншотами).

## TL;DR
- Общий вердикт: 🟢 всё зелёное / 🔴 есть падения
- Level 1: `N passed / M failed` (pytest) · `N passed / M failed` (vitest)
- Level 2: `X/Y сценариев PASS`
- Что чинить в первую очередь: <ссылка на раздел падений или «нет»>

## Level 1 — код-тесты
| Раннер | Команда | Passed | Failed | Время |
|---|---|---|---|---|
| pytest | `cd backend && python -m pytest -q` | | | |
| vitest | `cd frontend && npx vitest run` | | | |

Новые/затронутые тесты: <файлы>. Coverage (если снимался): baseline `X%` → `Y%`.

## Level 2 — UI-smoke (Playwright)
Стенд: `<https://jwork.jorchik.com | local preview>` · команда `cd frontend && npx playwright test`

| # | Сценарий | Вердикт | Скриншоты |
|---|---|---|---|
| 1 | <логин/открытие → создать → проверить → удалить> | 🟢 PASS / 🔴 FAIL | `frontend/e2e/screenshots/01-*.png` |
| 2 | | | |
| 3 | | | |

## Падения и где проблема
> Только если есть FAIL. По каждому — конкретное место, не «упало».

- **[Сценарий/тест]** — что сломалось → **предполагаемое место**: `file:line` / селектор `...` /
  фича не задеплоена / auth-гейт (Bearer) / сеть/таймаут. Флейки или реальный баг?

## Эскалация
- Реальные баги продукта (не флейки) → завести задачу в профиле **P2 (Баг)** с находкой как входом.
- Здесь код продукта НЕ чиним — это QA-гейт, не Fix.
