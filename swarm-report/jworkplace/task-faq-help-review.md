PASS

# Review — Фича «Справка» (FAQ по режимам), ветка jworkplace/task-faq-help

Ревьюер: `code-reviewer` (fresh agent, TS/React) + оркестратор. Прогнан vitest (98/98), build чист.

## Вердикт: PASS

- Новый `FaqPanel.tsx` — статический, без api-вызовов; `PanelHeader` (title «Справка»); 7 режимов +
  блок «Чат vs Поиск». Презентационный, рисков нет.
- `App.tsx`: `SUPPORT_TAB` → `SERVICE_TABS=[faq, support]` + `SERVICE_TAB_KEYS` + `isServiceTab()`;
  сервис-вкладки рендерятся из массива, отделены классом `tab-service`. `TAB_KEYS` включает `faq`
  → персист (T01) и клавиатурное кольцо/roving tabindex (T07) охватывают новую вкладку.
- `ActiveProjectIndicator` скрыт для обеих сервис-вкладок через `!isServiceTab(tab)` (было только
  `!== support`) — T09-инвариант обобщён корректно, не сломан.
- Сервисные вкладки доступны без активного проекта (нет EmptyState) — T08-инвариант соблюдён.
- Тест структуры таб-бара (T10) обновлён под 7 вкладок и новый порядок; +6 тестов «Справки»
  (присутствие, класс отделения, контент, индикатор скрыт, доступность без проекта).
- Doc-sync `frontend/CLAUDE.md` (таблица вкладок) обновлён.

Два мелких замечания ревью (устаревший doc-sync, лишний type-cast) — устранены сразу. Багов нет,
эскалаций (security/grounding) нет.
