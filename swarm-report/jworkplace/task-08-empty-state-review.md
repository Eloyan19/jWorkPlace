PASS

# Review — T08 единый empty-state проектных вкладок (ветка jworkplace/task-08-empty-state)

Ревьюер: `code-reviewer` (TS/React). Прогнан `tsc --noEmit` (чисто) + `vitest run` (83/83).

## Вердикт: PASS — багов не найдено

Проверенные риски и результат:
- **Инвариант mounted-panels:** `activeProjectId ? <Panel/> : <EmptyState/>` (App.tsx) размонтирует
  панель ТОЛЬКО при переходе id ↔ null; переключение между двумя ненулевыми проектами оставляет
  панель смонтированной — инвариант «переключение вкладок не теряет состояние» держится.
- **Отписка:** `subscribeActiveProject(setActiveProjectId)` возвращает unsubscribe как cleanup
  useEffect — утечки слушателя CustomEvent нет.
- **Синхронность dispatch + батчинг React 18:** десинка состояния App и панели в момент очистки
  проекта нет — окна для краша нет.
- **Удаление `!activeId`-веток из 6 панелей:** безопасно; `activeId`/`activeIdRef` продолжают
  использоваться в race-guard'ах, мёртвого кода нет. AgentPanel достижим только через 'edits'-таб,
  гейтированный `activeProjectId`.
- **T01 (персист вкладки) и T07 (клавиатурная навигация/roving tabindex):** этим диффом не затронуты.
- **Тесты:** старые per-panel «нет проекта → подсказка» заменены на проверку реального поведения в
  App.test.tsx (EmptyState виден, панель не смонтирована, support работает, панель рендерится при
  активном проекте) — не тавтология.

Ничего чинить не требуется; эскалаций (security/secret/grounding) нет.
