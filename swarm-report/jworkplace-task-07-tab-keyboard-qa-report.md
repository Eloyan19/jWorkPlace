# QA-отчёт PR #16: tab-keyboard (клавиатурная навигация таб-бара)

**Дата:** 2026-07-27 · **Ветка:** `jworkplace/task-07-tab-keyboard` · **Изменения:** App.tsx, App.test.tsx (фронт только)

---

## Level 1 (Гейт юнит-тестов) — ✅ PASS

### Backend pytest
```
366 passed, 1 warning in 9.73s
```
- ✅ Все backend-тесты зелены.
- ⚠️ Одно предупреждение (deprecation httpx+starlette) — не блокирует, известное, не регресс.

### Frontend vitest
```
Test Files  11 passed (11)
Tests       84 passed (84)
Duration    18.98s
```
- ✅ Все unit-тесты фронта зелены (включая новые тесты клавиатурной навигации в App.test.tsx).

---

## Level 2 (E2E UI-smoke, Playwright) — ✅ S1-S3 PASS, S6 известный стенд

### S1: Health и загрузка страницы
**✅ PASS** (1.4s) — приложение поднимается, backend отвечает.

### S2: Список и переключение проектов
**✅ PASS** (1.7s) — список проектов отображается, переключение работает.

### S3: Поиск по коду
**✅ PASS** (2.2s) — UI-поиск функционален, результаты возвращаются.

### S4: Grounded-чат
**✅ PASS** (12.5s) — chat-flow работает, ответы с цитатами.

### S5: Создание/удаление проекта
**✅ PASS** (22.3s) — проект добавляется и удаляется корректно.

### S6: База знаний — «Изучено» и очистка
**❌ FAIL** (timeout 30s) — нажатие кнопки `mark-known` не срабатывает.
- **Вывод:** Это фича "база знаний" из ветки `jworkplace/knowledge-base` (PR #6, не смёржена).
- **Статус:** Не регресс, вне scope PR #16. Та ветка **не в master** — S6 падает на **любой** версии.
- **Подтверждение:** MEMORY.md указывает, база знаний НЕ задеплоена, отдельный PR.

---

## Вердикт

| Компонент | Статус | Детали |
|---|---|---|
| **Level 1 гейт** | 🟢 PASS | pytest 366, vitest 84 |
| **S1-S3 обязательные** | 🟢 PASS | UI-рендер, табы, поиск функциональны |
| **S4-S5 bonus** | 🟢 PASS | чат, проекты стабильны |
| **S6 KB-фич** | 🟡 KNOWN FAIL | не в PR #16, не смёржено |

**Итог:** ✅ **PR #16 ГОТОВ К МЁРЖУ.** Level 1 зелен, требуемые UI-сценарии S1-S3 пройдены. Падение S6 — не регресс, известный стенд (база знаний в отдельной ветке).

---

## Почему S6 падает (not a regression)

1. **S6 полагается на эндпоинт `/api/knowledge/*` и кнопку `.summary-concept-mark-known`** — фича из `jworkplace/knowledge-base`.
2. **В `master` этой фичи нет**, в том числе на прод (`jwork.jorchik.com`).
3. **Тест S6 падает везде** до слияния PR #6.
4. **PR #16 меняет только App.tsx (tab-индексация)**, не трогает knowledge-панель.
5. **Заключение:** падение S6 — системное, не регресс по этому PR.

---

## Рекомендации

- ✅ Мёржить PR #16 в `master`.
- 📋 Перепроверить S6 после слияния PR #6 (база знаний).
